<?php
    // cookie -> often used to identify a user.
    // A cookie : a small file that the server embeds on the user's computer. 
    // setcookie(name, value, expire, path, domain, secure, httponly)
    // expire : the time at which the cookie expires.
    // path : the path on the server in which the cookie will be available.
    // domain : the domain in which the cookie will be available.
    // secure : if set to TRUE the cookie will only be sent over a secure protocol (HTTPS)
    // httponly : if set to TRUE the cookie will be inaccessible to JavaScript.

    // cookie is used to store data on the client side.

    // session : a server-side storage that is unique to each user.
    
?>